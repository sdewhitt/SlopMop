# All dependencies are consolidated in backend/requirements.txt.
# Use the backend virtual environment for both serving and training:
#   pip install -r ../../backend/requirements.txt
-r ../../backend/requirements.txt
